import Vue from 'vue'
import {
    Icon,
    CellGroup,
    Field,
    Button,
    Tabbar,
    TabbarItem,
    Search,
    Tab,
    Tabs,
    Swipe,
    SwipeItem,
    Checkbox,
    CheckboxGroup,
    Toast 
} from 'vant'

Vue.use(Icon).use(CellGroup).use(Field).use(Button ).use(Tabbar).use(TabbarItem ).use(Search).use(Tab).use(Tabs).use(Swipe).use(SwipeItem)
    .use(Checkbox).use(CheckboxGroup).use(Toast)